{
  "name": "regmar-portal",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "seed": "pnpm --filter server prisma:seed",
    "migrate": "pnpm --filter server prisma:migrate"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
